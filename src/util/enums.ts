export enum USER_ROLE_TYPE {
  EXECUTOR = "executor",
  TEAMLEAD = "teamlead",
}

export enum TASK_STATUS_VALUE {
  BACKLOG = "backlog",
  TODO = "todo",
  IN_PROGRESS = "in progress",
  ON_HOLD = "on hold",
  IN_REVIEW = "in review",
  DONE = "done",
  CANCELED = "canceled",
}
